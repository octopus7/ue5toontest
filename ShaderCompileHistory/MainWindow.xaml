<Window x:Class="ShaderCompileHistory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ShaderCompileHistory"
        Width="460"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None">
    <Window.Resources>
        <SolidColorBrush x:Key="ActiveBarBrush" Color="#8FBEEB" />
        <SolidColorBrush x:Key="BarTrackBrush" Color="#148FBEEB" />
        <SolidColorBrush x:Key="HistoryBarBrush" Color="#338FBEEB" />
        <Style x:Key="BarCapsuleStyle" TargetType="Border">
            <Setter Property="Height" Value="9" />
            <Setter Property="Background" Value="{StaticResource BarTrackBrush}" />
            <Setter Property="Margin" Value="0,0,0,5" />
        </Style>
    </Window.Resources>

    <Grid MouseLeftButtonDown="HandleDragMove">
        <Border Width="420"
                Background="#FFFDFDFD"
                BorderBrush="#FFF0F0F0"
                BorderThickness="1"
                CornerRadius="28"
                Padding="16,14,16,18"
                VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect BlurRadius="30"
                                  Color="#24000000"
                                  Opacity="1"
                                  ShadowDepth="0" />
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid Margin="0,6,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                               VerticalAlignment="Center"
                               FontFamily="Consolas"
                               FontSize="34"
                               FontWeight="Bold"
                               Foreground="#FF202020"
                               Text="{Binding ElapsedText}" />

                    <TextBlock Grid.Column="1"
                               Margin="16,0,12,0"
                               HorizontalAlignment="Right"
                               VerticalAlignment="Center"
                               FontFamily="Bahnschrift Light"
                               FontSize="12"
                               Foreground="#7F202020"
                               Text="Shader compile elapsed time" />

                    <Button x:Name="CloseButton"
                            Grid.Column="2"
                            Width="28"
                            Height="28"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Background="#FFE46B6B"
                            BorderBrush="#FFD65252"
                            BorderThickness="1"
                            Click="CloseButton_OnClick"
                            Cursor="Hand"
                            FocusVisualStyle="{x:Null}"
                            ToolTip="Close">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="999">
                                    <Viewbox Width="10"
                                             Height="10"
                                             HorizontalAlignment="Center"
                                             VerticalAlignment="Center">
                                        <Path Data="M 0,0 L 1,1 M 1,0 L 0,1"
                                              Stroke="White"
                                              StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round"
                                              StrokeThickness="0.22"
                                              Stretch="Fill" />
                                    </Viewbox>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <StackPanel Grid.Row="1">
                    <Grid Margin="0,0,0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="38" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="1"
                                Style="{StaticResource BarCapsuleStyle}"
                                Margin="0">
                            <Grid ClipToBounds="True">
                                <Border Background="{StaticResource BarTrackBrush}" />
                                <Border Background="{StaticResource ActiveBarBrush}"
                                        RenderTransformOrigin="0,0.5">
                                    <Border.RenderTransform>
                                        <ScaleTransform ScaleX="{Binding TopBarProgress}" />
                                    </Border.RenderTransform>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <ItemsControl ItemsSource="{Binding HistoryBars}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,5">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="38" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>

                                    <TextBlock VerticalAlignment="Center"
                                               FontFamily="Consolas"
                                               FontSize="11"
                                               Foreground="#66808C99"
                                               Text="{Binding SecondsLabel}" />

                                    <Border Grid.Column="1"
                                            Style="{StaticResource BarCapsuleStyle}"
                                            Margin="0">
                                        <Grid ClipToBounds="True">
                                            <Border Background="{StaticResource HistoryBarBrush}"
                                                    RenderTransformOrigin="0,0.5">
                                                <Border.RenderTransform>
                                                    <ScaleTransform ScaleX="{Binding Progress}" />
                                                </Border.RenderTransform>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
